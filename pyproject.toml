[project]
name = "hollow-cube-tic-tac-toe"
version = "0.1.0"
description = "A hollow cube tic tac toe with 5 algorithms play against each other."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "tqdm>=4.68.4",
]
